set -e

MAKE_OPTIONS="TARGET_OS=windows_opengl TARGET_ARCH=x86 MAKE_WITH_CONSOLE=true COLOR_TYPE=COLOR32BITS STYPE=PS_STYPE_FLOAT32"
make -j16 $MAKE_OPTIONS
make install $MAKE_OPTIONS APPNAME_SUFFIX=_console
